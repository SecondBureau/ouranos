.home_item_content
  %ul
    - @posts.each do |post|
      %li.post_title= link_to post.title, main_app.post_path(post)
      %li.post_content= truncate(post.content, :length => 325, :omission => '...')
      %li.post_split_line
