#categories
	%h3= t("category")
	%ul
		- @categories.each do |category|
			%li
				%span= link_to category.title, posts_with_category_path(category)
				%ul
					- category.posts.each do |post|
						%li= post.title
				
