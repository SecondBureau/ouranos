#side_bar
  #title= link_to t("blog.title"), main_app.posts_path
  - if can? :manage, Post
    #side_operations
      %ul
        %li= link_to "Add new post", admin.new_path(:post)
        %li= link_to "Manage posts", admin.list_path(:post)