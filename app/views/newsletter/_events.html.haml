%tr
  %td{:style => "padding:5px;"}
    %h2{:style => "margin:5px 0px;"} Latest events
- @events.each do |event|
  %tr
    %td{:style => "padding-left:20px;"}= link_to event.title, url_for(:controller => "events", :action => "show", :id => event.id, :locale => "en", :only_path => false)