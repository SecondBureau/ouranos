= semantic_form_for @comment, :url => main_app.polymorphic_path(@comment) do |form|
    = form.input :user_id, :as => :hidden, :value => current_user.id
    = form.input :post_id, :as => :hidden
    = form.input :content, :input_html => {:rows => 10, :cols =>50}
    = form.submit
    
